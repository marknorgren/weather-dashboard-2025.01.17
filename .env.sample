# To prevent accidentally leaking env variables to the client, 
# only variables prefixed with VITE_ are exposed to your Vite-processed code.
# https://vite.dev/guide/env-and-mode

# OpenWeatherMap API Configuration
VITE_OPENWEATHERMAP_API_KEY=your_api_key_here
VITE_OPENWEATHER_API_URL=https://api.openweathermap.org/data/2.5

# Optional Configurations
VITE_ENABLE_WEATHER_STUBS=false    # Enable/disable weather data stubbing
VITE_STUB_DELAY_MS=200            # Simulated network delay for stubs
NODE_ENV=development         # development/production
PORT=3000                    # Server port
